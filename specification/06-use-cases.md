# Use Cases

## UC-001 — Initialize a project

**Primary actor:** STAKE-001  
**Desired outcome:** Create a minimal validated project skeleton without network traffic.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-002 — Validate configuration and fixtures

**Primary actor:** STAKE-001  
**Desired outcome:** Identify every input error before attempting delivery.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-003 — Compile a realized run bundle

**Primary actor:** STAKE-002  
**Desired outcome:** Freeze exact bytes, identifiers, mutations, conditional attempts, and logical schedule.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-004 — Run conformance scenarios

**Primary actor:** STAKE-001  
**Desired outcome:** Execute planned deliveries and evaluate transport and receiver invariants.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-005 — Resume an interrupted run

**Primary actor:** STAKE-004  
**Desired outcome:** Continue known work and surface ambiguous attempts under an explicit policy.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-006 — Replay a realized run bundle

**Primary actor:** STAKE-001  
**Desired outcome:** Execute the frozen plan without invoking random generation or rediscovering fixtures.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-007 — Inspect a run

**Primary actor:** STAKE-004  
**Desired outcome:** Trace a verdict from scenario through event, delivery, attempt, observation, assertion, and evidence.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-008 — Regenerate reports

**Primary actor:** STAKE-002  
**Desired outcome:** Rebuild artifacts idempotently from the durable journal.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-009 — Configure signature behavior

**Primary actor:** STAKE-001  
**Desired outcome:** Generate valid and invalid provider-compatible signatures over exact bytes.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-010 — Connect a receiver observer

**Primary actor:** STAKE-003  
**Desired outcome:** Expose typed, read-only application evidence through a command or HTTP probe.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-011 — Diagnose a failing invariant

**Primary actor:** STAKE-001  
**Desired outcome:** Obtain a minimal causal report and, where supported, a reduced failing run bundle.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |

## UC-012 — Gate a pull request

**Primary actor:** STAKE-002  
**Desired outcome:** Run offline in CI, publish artifacts, and return a stable exit status.

| Aspect | Contract |
| --- | --- |
| Preconditions | Project files are readable; relevant dependencies and target/observer capabilities are available. |
| Nominal flow | Validate inputs, perform the named operation, persist evidence where applicable, and return a stable classified result. |
| Off-nominal flow | Reject invalid input before traffic; preserve partial evidence; classify receiver, environment, harness, unsupported, ambiguous, and cancelled outcomes separately. |
| Security | Apply destination, secret, path, process, output, and redaction policy before side effects. |
| Completion evidence | The mapped requirements and tests in the traceability index pass. |
